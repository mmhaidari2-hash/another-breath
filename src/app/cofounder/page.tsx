import CofounderGate from '@/components/CofounderGate';

export const metadata = {
  title: 'Co-founder access — Cladak',
  robots: { index: false, follow: false },
};

export default function CofounderPage() {
  return <CofounderGate />;
}
