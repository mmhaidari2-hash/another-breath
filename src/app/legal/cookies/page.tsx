import LegalShell from '@/components/LegalShell';

export default function CookiesPage() {
  return (
    <LegalShell title="Cookies">
      <p>
        Phase 1 uses essential storage only: language preference and watch/compare lists in
        localStorage. No third-party advertising cookies.
      </p>
      <p>If analytics are added later, this page will be updated before activation.</p>
    </LegalShell>
  );
}
